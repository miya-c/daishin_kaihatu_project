# 水道検針アプリ 画面遷移速度改善 TODOリスト

このリストは、水道検針アプリの画面遷移速度を改善するための作業項目と、推奨される実施順序を示しています。各項目が完了したら、次の項目に進んでください。

## フロントエンド (daishin_kaihatu_project) の改善

### 1. ビルドプロセスの導入と最適化（最優先） ✅
- **タスク:** Viteなどのモダンなビルドツールを導入し、Reactアプリケーションを本番用に最適化されたJavaScriptバンドルに変換する。
    - JSXの事前トランスパイル、コードの圧縮、ツリーシェイキング、コード分割を適用する。
- **完了後:** このタスクが完了したら、次のタスクへ取り掛かってください。

### 2. CSSの最適化 ✅
- **タスク:** 全てのCSSファイルを統合・バンドルし、ミニファイする。クリティカルCSSのインライン化を検討し、不要なCSSを削除する。`waitForStylesLoaded`関数が不要になるか評価する。
- **完了後:** このタスクが完了したら、次のタスクへ取り掛かってください。

### 3. 人工的な遅延の削除 ✅
- **タスク:** `property_select.html` 内の `handlePropertySelect` 関数にある `setTimeout(() => { window.location.href = targetUrl; }, 300);` を削除する。
- **完了後:** このタスクが完了したら、次のタスクへ取り掛かってください。

### 4. アニメーションの最適化 ✅
- **タスク:** 物件カードの `animation-delay` を見直し、必要であれば簡素化または削除する。CSS `will-change` プロパティの適用を検討する。
- **完了後:** このタスクが完了したら、次のタスクへ取り掛かってください。

### 5. PWA関連スクリプトの整理 ✅
- **タスク:** `manifest.json` とサービスワーカーの登録、および関連するメタタグの読み込みをアプリケーションのエントリポイントに集約し、重複を排除する。
- **完了後:** このタスクが完了したら、次のタスクへ取り掛かってください。

### 6. その他のフロントエンド最適化 ✅
- **タスク:** 画像の最適化（WebP変換、リサイズ、遅延読み込み）、フォントの最適化（`font-display: swap`、サブセット化）、DOM操作の効率化（Reactの最適化フック利用）を検討し、適用する。
- **完了後:** このタスクが完了したら、次のタスクへ取り掛かってください。

## バックエンド (merter-library-test - GAS/Google Spreadsheet) の改善

### 7. GASスクリプトの実行速度最適化
- **タスク:** `getProperties` および `getRooms` 関数において、バッチ処理（`getValues()`）の活用、不要なログ出力の削除、GASのCache Serviceの利用、トリガーの最適化、実行時間制限の確認を行う。
- **完了後:** このタスクが完了したら、次のタスクへ取り掛かってください。

### 8. Google Spreadsheetのパフォーマンス最適化
- **タスク:** スプレッドシートのデータ構造の最適化、複雑な計算式の見直し、条件付き書式やデータ検証ルールの削減、シートの分割を検討する。
- **完了後:** このタスクが完了したら、次のタスクへ取り掛かってください。

### 9. ネットワークリクエストの最適化
- **タスク:** API呼び出しの削減、必要なデータの事前取得（プリフェッチ）の検討、レスポンスデータの最小化を行う。
- **完了後:** このタスクが完了したら、次のタスクへ取り掛かってください。

## 全体的なアプローチ

### 10. パフォーマンス計測と評価
- **タスク:** Google ChromeのLighthouse、Performanceタブ、Networkタブなどを活用し、具体的なボトルネックを数値で特定する。改善策を適用する前と後でパフォーマンスを計測し、効果を定量的に評価する。
- **完了後:** このタスクが完了したら、次のタスクへ取り掛かってください。

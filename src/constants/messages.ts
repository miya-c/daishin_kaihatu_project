/**
 * Shared UI message constants.
 * Centralises hardcoded display strings so they are defined in one place.
 */

/** Placeholder when a value is not available */
export const NOT_AVAILABLE = 'N/A';

/** Fallback room name when data is missing */
export const ROOM_NAME_UNKNOWN = '部屋名不明';

/** Fallback property name when data is missing */
export const PROPERTY_NAME_UNKNOWN = '物件名不明';

/** Fallback property name for header display */
export const PROPERTY_NAME_UNSET = '物件名未設定';

/** Fallback room name for header display */
export const ROOM_NAME_UNSET = '部屋名未設定';

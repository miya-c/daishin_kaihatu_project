# 検針システム パフォーマンス改善提案

## 現状分析

### 検針情報画面の課題
- 大きなHTMLファイル（26,586トークン）による初期読み込み時間の増加
- 画面表示のたびにAPIリクエストが発生（キャッシュ不足）
- 複雑なReact.useEffectの依存関係による不要なレンダリング
- コンポーネントが単一ファイルに集約されている

### パフォーマンス改善案

## 1. フロントエンド最適化

### 1.1 レイジーロード（遅延読み込み）の実装
```javascript
// React.lazyを使用したコンポーネントの遅延読み込み
const MeterReadingApp = React.lazy(() => import('./components/MeterReadingApp'));
const RoomList = React.lazy(() => import('./components/RoomList'));

// Intersection Observer APIを使用した画像の遅延読み込み
const LazyImage = ({ src, alt, className }) => {
  const [isLoaded, setIsLoaded] = React.useState(false);
  const imgRef = React.useRef();

  React.useEffect(() => {
    const observer = new IntersectionObserver(
      ([entry]) => {
        if (entry.isIntersecting) {
          setIsLoaded(true);
          observer.disconnect();
        }
      },
      { threshold: 0.1 }
    );

    if (imgRef.current) {
      observer.observe(imgRef.current);
    }

    return () => observer.disconnect();
  }, []);

  return (
    <img
      ref={imgRef}
      src={isLoaded ? src : '/placeholder.svg'}
      alt={alt}
      className={className}
    />
  );
};
```

### 1.2 コンポーネント分割と最適化
```javascript
// メモ化されたコンポーネント
const MeterReadingItem = React.memo(({ reading, onUpdate }) => {
  const [value, setValue] = React.useState(reading.value);
  
  const handleUpdate = React.useCallback(() => {
    onUpdate(reading.id, value);
  }, [reading.id, value, onUpdate]);

  return (
    <div className="meter-reading-item">
      <input
        type="number"
        value={value}
        onChange={(e) => setValue(e.target.value)}
        onBlur={handleUpdate}
      />
    </div>
  );
});

// 仮想化されたリスト
const VirtualizedRoomList = React.memo(({ rooms, onSelect }) => {
  const [visibleStart, setVisibleStart] = React.useState(0);
  const [visibleEnd, setVisibleEnd] = React.useState(10);

  const visibleRooms = React.useMemo(() => {
    return rooms.slice(visibleStart, visibleEnd);
  }, [rooms, visibleStart, visibleEnd]);

  return (
    <div className="virtualized-list">
      {visibleRooms.map((room) => (
        <RoomItem key={room.id} room={room} onSelect={onSelect} />
      ))}
    </div>
  );
});
```

### 1.3 キャッシュ戦略の改善
```javascript
// IndexedDBを使用したオフラインキャッシュ
class CacheManager {
  constructor() {
    this.dbName = 'MeterReadingCache';
    this.version = 1;
    this.db = null;
  }

  async init() {
    return new Promise((resolve, reject) => {
      const request = indexedDB.open(this.dbName, this.version);
      
      request.onerror = () => reject(request.error);
      request.onsuccess = () => {
        this.db = request.result;
        resolve();
      };
      
      request.onupgradeneeded = (event) => {
        const db = event.target.result;
        
        // 物件キャッシュ
        if (!db.objectStoreNames.contains('properties')) {
          db.createObjectStore('properties', { keyPath: 'id' });
        }
        
        // 部屋キャッシュ
        if (!db.objectStoreNames.contains('rooms')) {
          const roomStore = db.createObjectStore('rooms', { keyPath: 'id' });
          roomStore.createIndex('propertyId', 'propertyId');
        }
        
        // 検針データキャッシュ
        if (!db.objectStoreNames.contains('meterReadings')) {
          const readingStore = db.createObjectStore('meterReadings', { keyPath: 'id' });
          readingStore.createIndex('roomId', 'roomId');
          readingStore.createIndex('timestamp', 'timestamp');
        }
      };
    });
  }

  async cacheMeterReadings(roomId, readings) {
    const transaction = this.db.transaction(['meterReadings'], 'readwrite');
    const store = transaction.objectStore('meterReadings');
    
    for (const reading of readings) {
      await store.put({
        ...reading,
        roomId,
        timestamp: Date.now()
      });
    }
  }

  async getCachedMeterReadings(roomId) {
    const transaction = this.db.transaction(['meterReadings'], 'readonly');
    const store = transaction.objectStore('meterReadings');
    const index = store.index('roomId');
    
    return new Promise((resolve) => {
      const request = index.getAll(roomId);
      request.onsuccess = () => resolve(request.result);
    });
  }
}

// Service Workerを使用したネットワークレベルのキャッシュ
// sw.js
self.addEventListener('fetch', (event) => {
  if (event.request.url.includes('/api/')) {
    event.respondWith(
      caches.open('api-cache-v1').then((cache) => {
        return fetch(event.request).then((response) => {
          // GETリクエストのみキャッシュ
          if (event.request.method === 'GET') {
            cache.put(event.request, response.clone());
          }
          return response;
        }).catch(() => {
          // ネットワークエラー時はキャッシュから返す
          return cache.match(event.request);
        });
      })
    );
  }
});
```

## 2. バックエンド最適化

### 2.1 APIレスポンス時間の短縮
```javascript
// GASでのバッチ処理最適化
function getMeterReadings(propertyId, roomId) {
  const startTime = new Date().getTime();
  
  try {
    // スプレッドシートアクセスの最適化
    const sheet = SpreadsheetApp.openById(SPREADSHEET_ID);
    const dataRange = sheet.getRange('A:Z'); // 必要な範囲のみ取得
    const values = dataRange.getValues();
    
    // フィルタリング処理を最適化
    const filteredData = values.filter(row => 
      row[0] === propertyId && row[1] === roomId
    );
    
    // レスポンス時間をログに記録
    const endTime = new Date().getTime();
    console.log(`API実行時間: ${endTime - startTime}ms`);
    
    return {
      success: true,
      data: filteredData,
      responseTime: endTime - startTime
    };
  } catch (error) {
    console.error('API実行エラー:', error);
    return {
      success: false,
      error: error.message
    };
  }
}

// データベースアクセス最適化（可能な場合）
function optimizedGetMeterReadings(propertyId, roomId) {
  // インデックスを使用した高速検索
  const query = `
    SELECT * FROM meter_readings 
    WHERE property_id = ? AND room_id = ?
    ORDER BY created_at DESC
    LIMIT 100
  `;
  
  return executeQuery(query, [propertyId, roomId]);
}
```

### 2.2 データ圧縮と最適化
```javascript
// JSON圧縮
function compressResponse(data) {
  // 不要なフィールドを削除
  const compressed = data.map(item => ({
    id: item.id,
    val: item.value,
    ts: item.timestamp,
    st: item.status
  }));
  
  return JSON.stringify(compressed);
}

// ページネーション実装
function getPaginatedMeterReadings(propertyId, roomId, page = 1, limit = 50) {
  const offset = (page - 1) * limit;
  
  const totalCount = getTotalMeterReadingsCount(propertyId, roomId);
  const data = getMeterReadingsWithPagination(propertyId, roomId, offset, limit);
  
  return {
    data,
    pagination: {
      page,
      limit,
      totalCount,
      totalPages: Math.ceil(totalCount / limit),
      hasNextPage: page * limit < totalCount
    }
  };
}
```

## 3. ユーザーエクスペリエンス向上

### 3.1 プリロード戦略
```javascript
// 次の画面のデータを事前読み込み
const usePreloadNextRoom = (currentRoomIndex, allRooms) => {
  React.useEffect(() => {
    const nextRoomIndex = currentRoomIndex + 1;
    if (nextRoomIndex < allRooms.length) {
      const nextRoom = allRooms[nextRoomIndex];
      // 次の部屋のデータを事前読み込み
      preloadMeterReadings(nextRoom.propertyId, nextRoom.id);
    }
  }, [currentRoomIndex, allRooms]);
};

// 画像の事前読み込み
const preloadImages = (imageUrls) => {
  imageUrls.forEach(url => {
    const img = new Image();
    img.src = url;
  });
};
```

### 3.2 レスポンシブデザイン最適化
```css
/* モバイルファースト設計 */
@media (max-width: 768px) {
  .meter-reading-grid {
    grid-template-columns: 1fr;
    gap: 8px;
  }
  
  .meter-reading-item {
    padding: 12px;
    font-size: 16px;
  }
}

/* タブレット対応 */
@media (min-width: 769px) and (max-width: 1024px) {
  .meter-reading-grid {
    grid-template-columns: repeat(2, 1fr);
    gap: 12px;
  }
}

/* デスクトップ対応 */
@media (min-width: 1025px) {
  .meter-reading-grid {
    grid-template-columns: repeat(3, 1fr);
    gap: 16px;
  }
}
```

## 4. 実装優先度

### 高優先度（即効性が高い）
1. **コンポーネントのメモ化**: React.memo、useMemo、useCallbackの適用
2. **APIキャッシュ**: sessionStorageの効率的な活用
3. **不要なレンダリング削減**: useEffectの依存関係最適化
4. **画像の遅延読み込み**: Intersection Observer APIの活用

### 中優先度（開発工数とのバランス）
1. **コンポーネント分割**: 大きなファイルを小さく分割
2. **仮想化**: 大きなリストの仮想化
3. **Service Worker**: オフライン対応とキャッシュ戦略
4. **プリロード**: 次画面データの事前読み込み

### 低優先度（長期的な改善）
1. **IndexedDB**: 本格的なオフラインキャッシュ
2. **PWA機能強化**: プッシュ通知、背景同期
3. **バックエンドDB移行**: GoogleスプレッドシートからDBへの移行
4. **CDN活用**: 静的リソースの配信最適化

## 5. 測定・監視

### 5.1 パフォーマンス指標
- **FCP (First Contentful Paint)**: 初回コンテンツ描画時間
- **LCP (Largest Contentful Paint)**: 最大コンテンツ描画時間
- **CLS (Cumulative Layout Shift)**: 累積レイアウトシフト
- **FID (First Input Delay)**: 初回入力遅延

### 5.2 監視ツール
```javascript
// パフォーマンス測定
const measurePerformance = () => {
  const observer = new PerformanceObserver((list) => {
    const entries = list.getEntries();
    entries.forEach(entry => {
      console.log(`${entry.name}: ${entry.duration}ms`);
    });
  });
  
  observer.observe({ entryTypes: ['measure', 'navigation'] });
};

// APIレスポンス時間の測定
const measureAPIResponse = async (url) => {
  const startTime = performance.now();
  const response = await fetch(url);
  const endTime = performance.now();
  
  console.log(`API応答時間: ${endTime - startTime}ms`);
  return response;
};
```

## 6. 期待される効果

### 6.1 定量的効果
- **初期読み込み時間**: 30-50%短縮
- **画面遷移時間**: 40-60%短縮
- **APIレスポンス体感時間**: 50-70%短縮
- **メモリ使用量**: 20-30%削減

### 6.2 定性的効果
- **ユーザー満足度向上**: スムーズな操作感
- **作業効率向上**: 検針作業の時間短縮
- **エラー率低下**: 安定したデータ取得
- **オフライン対応**: 電波状況に左右されない操作

## 7. 実装スケジュール

### フェーズ1（1-2週間）
- React.memo、useMemo、useCallbackの適用
- 不要なレンダリングの削減
- 画像の遅延読み込み実装

### フェーズ2（2-3週間）
- コンポーネント分割
- APIキャッシュ戦略の改善
- Service Workerの導入

### フェーズ3（3-4週間）
- 仮想化の実装
- IndexedDBキャッシュの構築
- プリロード機能の実装

### フェーズ4（継続的）
- パフォーマンス監視
- 最適化の継続的改善
- 新機能追加時の性能維持

この提案により、検針システムの全体的なパフォーマンスが大幅に向上し、ユーザーエクスペリエンスが改善されることが期待されます。